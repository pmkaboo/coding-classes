#^ `.odd?` and `.even?`

# ruby provides convenience methods for checking if a number is odd or even
# out of the box, so there is no need to do modulo operations for this

puts "1 is odd => #{1.odd?}"
puts "2 is even => #{2.even?}"